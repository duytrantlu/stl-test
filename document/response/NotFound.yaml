description: 'Resource not found.'
content:
  application/json:
    schema:
      description: NotFound
      type: object
      properties:
        name:
          type: string
        message:
          type: string
      example:
        name: NotFound
        message: The resource you requested was not found.
